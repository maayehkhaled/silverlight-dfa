% minimalbook.sty
% only for xelatex

%\LoadClass[pdftex
%,10pt
%,a4paper
%,DIV9
%,BCOR=1cm
%,chapterprefix
%,appendixprefix=true
%,toctotoc
%,liststotoc         % Tabellen & Abbildungsverzeichnis ins TOC
%,idxtotoc           % Index ins TOC
%,bibtotoc           % Bibliographie ins TOC
%,bibtotocnumbered   % Bibliographie im TOC nummeriert
%,liststotocnumbered % Alle Verzeichnisse im TOC nummeriert 
%]{scrbook}

% xetex spezifishe
\usepackage{amssymb} % this should be included before the package xunicode
\usepackage{xunicode}
\usepackage{fontspec}
\usepackage{xltxtra}
% %%%%%%%%%%%%%%%%%%%%

\usepackage{polyglossia}
	\setdefaultlanguage[spelling=new]{german}
	\DeclareUTFcharacter[\UTFencname]{x201C}{\grqq}
	\DeclareUTFcharacter[\UTFencname]{x201E}{\glqq}


% Hypertex
\RequirePackage[
 colorlinks=false
,pdfborder={0 0 0}
,setpagesize=false]{hyperref}

% Graphic
\RequirePackage{graphicx}
\RequirePackage{url}


% Math fonts and packages
\RequirePackage{%
	amsfonts
	,amsmath
	,amsthm
}

% Typography
%\RequirePackage{microtype}
\RequirePackage{colortbl}
\RequirePackage{lmodern}
\RequirePackage{soul}
\setlength\minrowclearance{3.5pt}
%\RequirePackage{inconsolata}
%\RequirePackage[scaled]{beramono}
\RequirePackage{oldstyle}
\RequirePackage{float}
\RequirePackage{array}

\RequirePackage{makeidx}
\makeindex



